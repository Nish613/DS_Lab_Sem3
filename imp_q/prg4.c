/*Menu Driven Program 
1.⁠ ⁠To insert a file name(.txt) to the binary search tree. 
Insertion takes place based on the alphabetical order. 
2.⁠ ⁠Insert directory to bst same as above 
3.⁠ ⁠To view a file to see if it exists 
4.⁠ ⁠Delete a file from bst. 
Every time you view it is appended to a double linked list. 
A file cannot have a file or directory as its child. 
It gives an error message otherwise. 
Now the directory and file name are just strings. 
To segregate between dir and TXT . 
TXT will have .txt at the end.*/

#include<stdio.h>
#include<stdlib.h>
#include<string.h>

